   <%# <%= form_tag create_author_quotes_path do %> 
     
        <%# <%= label_tag :description%>
        <%# <%= text_area_tag :description%> 
  
   <%# <%= submit_tag "Add"%> 
<%# <%end%>


<% if flash[:errors]%>
    <% flash[:errors].each do |error| %>
        <div class="alert alert-danger" role="alert">
                <%= error%>
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    <% end %>
<%end%>

<div class="container mt-5 mr-5">
    <div class="col-md-6 mb-4">
        <div class="card">
            <div class="card-body">
                 <%= form_tag create_author_quotes_path do %>
                         <h3 class="text-center font-bold py-4"><strong>Create New Quote</strong></h3>
                                <div class="input-group mb-3">
                                        <div class="input-group-prepend">
                                             <span class="input-group-text" id="inputGroup-sizing-default"><i class="fas fa-user"></i></span>
                                        </div>
                                            <%= text_field_tag  :description, nil,placeholder: 'Enter Quote...',class: "form-control" %>                  
                                </div>
                                  <%= select_tag(:tag_ids, options_from_collection_for_select(Tag.all, :id, :name), include_blank: "Please select a tag...",class: "form-control") %>
                                  <%# <%= collection_select :name, :tag_ids,@tags.map{|t| [t.name,t.id]}, @tags.stores.pluck(:id),:name,multiple: true %> 
                                
                                <div class="text-center py-4">
                                    <%= submit_tag "Create Quote", class: "btn btn-primary" %>
                                </div>
                 <%end%>         
            </div>
        </div>
    </div>
</div>